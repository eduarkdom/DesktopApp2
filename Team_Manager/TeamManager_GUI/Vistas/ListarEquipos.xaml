<Window x:Class="TeamManager_GUI.Vistas.ListarEquipos"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeamManager_GUI.Vistas"
        mc:Ignorable="d"
        Title="ListarEquipos" Height="600" Width="1000" Activated="Window_Activated">
    <Grid x:Name="gListarEquipos">
        <DataGrid FontSize="16" x:Name="dgListadoEquipos" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Actualizar" x:Name="dgtcActualizar">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:Name="dtActualizar">
                            <Button x:Name="btnActualizar" Content="Actualizar" Click="btnActualizar_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Eliminar" x:Name="dgtcEliminar">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:Name="dtEliminar">
                            <Button x:Name="btnEliminar" Content="Eliminar" Click="btnEliminar_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
